services:
  thrasherverse-fivem:
    image: fivem
    container_name: thrasherverse-fivem
    restart: unless-stopped
    ports:
      - 30120:30120/tcp
      - 30120:30120/udp
      - 40120:40120/tcp
    volumes:
      - ${SERVER_DATA}:/home/fivem/server-data